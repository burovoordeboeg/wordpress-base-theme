{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker-compose",
            "command": "docker stop $(docker ps -qa) && docker-compose up -d",
            "args": [],
            "type": "shell"
        },
        {
            "label": "docker-close",
            "command": "docker stop $(docker ps -qa)",
            "args": [],
            "type": "shell"
        }
    ]
}