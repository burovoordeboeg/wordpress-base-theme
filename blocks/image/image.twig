{% extends "base/block.twig"%}

{% set context = {
  data: {
	image: fields.image.image,
	alt: image.alt,
	size: fields.image.size,
	variant: fields.image.aspect_ratio,
	align: align_text,
  },
  align: {
	  left:'mr-auto',
	  right:'ml-auto',
	  center:'mx-auto',
  }
} %}


{% block block_content %}

	{{wp_get_attachment_image(context.data.image.id, context.data.size, '', {'class' : context.variants[context.data.variant] ~ context.align[context.data.align]})|raw}}

{% endblock %}
