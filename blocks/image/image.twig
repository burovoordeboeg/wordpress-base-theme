{% extends "base/block.twig"%}

{# TODO Remove/change custom class based on project #}

{% set image = fields.image.image %}
{% set ratio = fields.image.ratio %}
{% set caption = wp_get_attachment_caption(image.id) %}

{% block block_content %}

	{{include('components/media/image.twig', {
		data: {
			id: image.id,
			size: 'large',
			ratio: ratio,
			caption: caption,
			position: {
				top: image.top,
				left: image.left
			}
		},
		classes: {
			custom: 'my-8'
		}
	}, with_context = false)}}

{% endblock %}
