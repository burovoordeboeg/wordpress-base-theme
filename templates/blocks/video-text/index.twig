{% extends "blocks/image-text/index.twig" %}

{% block media %}
	{% if fields.video.video_source == 'url' %}
		{{include('includes/video.twig', {
			data: fields.video
		}, with_context = false)}}
	{% else %}
		{{include('includes/video-oembed.twig', {
			data: fields.video
		}, with_context = false)}}
	{% endif %}
{% endblock %}
