{% extends "base/base.twig" %}
{% import "macros/icons.twig" as icon %}


{% block content %}
	<div class="text-blue-500 p-8">
		{{ icon.icon('arrow-down-on-square') }}
		{{ icon.icon('arrow-down-on-square', { width: 64 }) }}
		{{ icon.icon('arrow-down-on-square', {class: 'text-green-500'}) }}
	</div>

	{{object.get_content()|raw}}
{% endblock %}
