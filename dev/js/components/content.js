(function ($) {

	// // Truncate strings
	// setTimeout(function () {
	//     $('.js-truncate').dotdotdot({
	//         wrap: 'word',
	//         watch: window,
	//         after: '.js-readmore-handle'
	//     });
	// }, 200);

	// var text = document.querySelector('.js-truncate');
	// text.textContent = text.textContent.slice(0, 200).trim().concat('...')

})(jQuery);